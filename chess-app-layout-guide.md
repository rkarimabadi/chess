# راهنمای چیدمان (Grid & Layout) صفحات پلتفرم شطرنج آنلاین

> این سند مکمل «راهنمای عامل هوش مصنوعی برای UI به سبک اپل» است. در حالی که آن سند به **تایپوگرافی، رنگ و الگوهای تعاملی** می‌پرداخت، این سند به **معماری چیدمان صفحه (grid)** برای هر بخش از محصول شطرنج فارسی می‌پردازد — با اقتباس از اصول کتاب مرجع طراحی گرید *Layout Essentials* اثر بث توندرو (Beth Tondreau)، تطبیق‌یافته با نیازهای یک اپلیکیشن وب واکنش‌گرا، RTL و مبتنی بر Blazor + CSS خالص.

منبع محصول: مستند «پلتفرم بازی آنلاین شطرنج» (نسخه ۲.۳.۰).

---

## ۱. واژگان گرید — تطبیق با وب

کتاب مرجع، هر گرید را از شش عنصر پایه می‌سازد. جدول زیر معادل هرکدام را در بستر یک صفحه وب مشخص می‌کند تا عامل هوش مصنوعی هنگام پیاده‌سازی CSS، همان مفهوم را بشناسد:

| عنصر گرید | تعریف مفهومی | معادل در CSS/صفحه وب |
| :--- | :--- | :--- |
| **ستون (Column)** | نوار عمودی نگه‌دارندهٔ متن یا تصویر | `grid-template-columns` یا کانتینر flex عمودی |
| **مدول (Module)** | واحد تکرارشونده با فاصلهٔ ثابت، سازندهٔ ردیف/ستون | کارت‌ها، سلول‌های `grid` (مثل کارت‌های داشبورد) |
| **حاشیه (Margin)** | فاصلهٔ حائل بین لبهٔ صفحه/دستگاه و محتوا | `padding` بیرونی صفحه/کانتینر اصلی |
| **منطقهٔ فضایی (Spatial Zone)** | گروهی از مدول‌ها/ستون‌ها که یک ناحیهٔ کارکردی می‌سازند | ناحیه‌های معنایی صفحه (نوار بازی، سایدبار تاریخچه، نوار وضعیت) |
| **فلولاین (Flowline)** | تراز افقی نامرئی که چشم را در طول صفحه هدایت می‌کند | ردیف‌های `grid-template-rows` یا نقاط تراز عمودی بین بخش‌ها هنگام اسکرول |
| **مارکر (Marker)** | عنصر ثابت کمکی ناوبری (شماره صفحه، سربرگ) | breadcrumb، عنوان چسبان (sticky header)، badge وضعیت |

**قاعده برای عامل:** پیش از کدنویسی هر صفحه، این شش عنصر را برای همان صفحه به‌صراحت تعریف کن (در کامنت بالای فایل CSS Isolation همان کامپوننت) — نه اینکه گرید را به‌صورت انتزاعی و بدون نام‌گذاری بسازی.

---

## ۲. چگونه گرید مناسب هر صفحه را انتخاب کنیم

طبق منطق کتاب مرجع، انتخاب گرید باید بر اساس چهار پرسش انجام شود، نه سلیقه:

1. **ماهیت محتوا چیست؟** (متن روان / داده متراکم / یک عنصر تعاملی غالب مثل صفحه شطرنج)
2. **چقدر از آن وجود دارد؟** (یک فرم کوتاه در برابر جدول ۵۰ ردیفی گزارش‌های Staff)
3. **هدف صفحه چیست؟** (تمرکز بر یک اقدام / مرور و مقایسه / نظارت و کنترل)
4. **مخاطب کیست؟** (بازیکن عادی در فضای آرام برند «Peace» / کارمند Staff در فضای کاری با تراکم اطلاعاتی بالا)

بر همین اساس، سه خانوادهٔ گرید برای کل محصول تعریف می‌شود:

| خانوادهٔ گرید | صفحاتی که استفاده می‌کنند | منطق انتخاب |
| :--- | :--- | :--- |
| **تک‌ستونی متمرکز** | Landing، Auth، صفحهٔ نتیجهٔ بازی، Empty states | محتوا کم، هدف واحد، حس آرام و بدون حواس‌پرتی |
| **مدولار (Modular)** | Home/داشبورد، History، Leaderboard، Staff Dashboard | داده‌های تکرارشونده و قابل‌مقایسه (کارت بازی، ردیف کاربر) که باید در چانک‌های مساوی سازمان‌دهی شوند |
| **دوستونی نامتقارن + سلسله‌مراتبی** | صفحهٔ بازی (Game Room)، بازبینی تاریخچه، Staff Detail | یک عنصر غالب (صفحه شطرنج) در برابر اطلاعات فرعی (تاریخچه حرکات، پروفایل)؛ گرید باید وزن بصری نابرابر را نشان دهد |

---

## ۳. چیدمان صفحه‌به‌صفحه

### ۳.۱ لندینگ و Auth (`/`, `/auth/*`)

- **گرید:** تک‌ستونی، عرض محتوا محدود (حداکثر ~۴۸۰px روی دسکتاپ)، حاشیهٔ بیرونی سخاوتمندانه.
- **منطق:** طبق اصل «فضا به‌عنوان عنصر گرافیکی» در کتاب مرجع، خالی گذاشتن فضای اطراف فرم ورود، حس اعتماد و آرامش برند «Peace» را منتقل می‌کند؛ شلوغی این‌جا هزینهٔ تبدیل کاربر را بالا می‌برد.
- **فلولاین:** لوگو → عنوان کوتاه → فرم → CTA ثانویه؛ یک مسیر عمودی واحد بدون انشعاب.

### ۳.۲ داشبورد خانه (`/home`)

- **گرید:** مدولار، ۲ تا ۴ کارت هم‌عرض (شروع بازی ریت‌شده، بازی دوستانه، دعوت دوست، تاریخچه) که در موبایل به تک‌ستون تبدیل می‌شوند.
- **منطق:** این چهار CTA همان «مدول‌های تکرارشونده با فاصلهٔ ثابت» هستند؛ کتاب تأکید می‌کند نیازی نیست همهٔ مدول‌ها با محتوای سنگین پر شوند — یکی از کارت‌ها می‌تواند فقط آمار خلاصه با فضای خالی بیشتر باشد تا از یکنواختی بصری جلوگیری شود.
- **منطقهٔ فضایی جدا:** «آمار خلاصه» به‌صورت یک نوار افقی جدا (flowline) زیر کارت‌های CTA قرار می‌گیرد، نه داخل همان شبکهٔ کارتی.

### ۳.۳ صف بازی (`/play/queue`)

- **گرید:** تک‌ستونی متمرکز، شبیه به Auth.
- **منطق:** حالت انتظار باید کمترین محرک بصری را داشته باشد؛ فقط وضعیت صف، دکمهٔ لغو، و پیشنهاد جایگزین (تغییر کنترل زمان/دعوت دوست) — طبق اصل حالت خالی/انتظار در سند نیازمندی.

### ۳.۴ صفحهٔ بازی (`/game/:id`) — مهم‌ترین صفحهٔ محصول

این صفحه باید دقیقاً سلسله‌مراتب بصری تعریف‌شده در سند نیازمندی (بخش ۱۵.۲) را با یک گرید نامتقارن نمایش دهد:

**دسکتاپ — دو ستون نامتقارن:**

```
┌───────────────────────────────┬──────────────────┐
│                                │  ساعت حریف        │
│                                │  ─────────────    │
│        صفحهٔ شطرنج              │  تاریخچهٔ حرکات    │
│        (ستون غالب،             │  (اسکرول‌پذیر)      │
│         مربع، بدون رقابت با     │  ─────────────    │
│         عناصر دیگر)             │  Material گرفته‌شده │
│                                │  ─────────────    │
│                                │  ساعت خودم         │
│                                │  ─────────────    │
│                                │  اقدامات: تساوی/   │
│                                │  تسلیم/پیام آماده  │
└───────────────────────────────┴──────────────────┘
```

- ستون صفحهٔ شطرنج ≈ ۶۰–۶۵٪ عرض، ستون سایدبار ≈ ۳۵–۴۰٪ — نسبت مشابه گرید دوستونی نامتقارن کتاب مرجع که ستون بزرگ‌تر معمولاً حدود دو برابر ستون کوچک‌تر است.
- چون RTL است، صفحهٔ شطرنج در **سمت راست** (منطقی: `inline-start` در RTL) و سایدبار در چپ قرار می‌گیرد، مگر تصمیم UX خلاف آن باشد.
- هیچ مدولی نباید اندازهٔ صفحهٔ شطرنج را به رقابت بکشد؛ طبق اصل Deference و اصل کتاب دربارهٔ اندازهٔ تصویر به‌عنوان نشانهٔ اهمیت، صفحهٔ شطرنج همیشه بزرگ‌ترین عنصر گرید است.

**موبایل — تک‌ستونی با فلولاین عمودی:**

```
صفحهٔ شطرنج (حداکثر عرض مفید)
──────────────
نوار فشردهٔ وضعیت (ساعت‌ها + نوبت)
──────────────
[کشو/شیت پایینی: تاریخچهٔ حرکات + اقدامات]
```

- تاریخچه و اقدامات به bottom sheet منتقل می‌شوند (طابق سند اول، بخش ۶.۵)، نه این‌که به‌صورت فشرده زیر صفحه چپانده شوند.

### ۳.۵ نتیجهٔ بازی (`/game/:id/result`)

- **گرید:** تک‌ستونی متمرکز با یک نقطهٔ کانونی (نتیجه + دلیل پایان)، سپس ردیف افقی از اقدامات بعدی (rematch، بازگشت به خانه، بازبینی).
- **منطق:** طبق اصل ریتم و جریان، این صفحه نقطهٔ «مکث» در تجربه است — باید فضای تنفس بیشتری نسبت به صفحهٔ بازی داشته باشد تا حس پایان یک چرخه را منتقل کند.

### ۳.۶ تاریخچه و بازبینی (`/history`, `/history/:id`)

- **لیست تاریخچه:** گرید مدولار — هر بازی یک ردیف/کارت با اطلاعات یکسان (حریف، نتیجه، تاریخ، کنترل زمان) در ستون‌های هم‌تراز؛ در دسکتاپ می‌تواند به‌صورت جدول با ستون‌های تعریف‌شده باشد، در موبایل به کارت‌های عمودی تبدیل شود.
- **بازبینی یک بازی:** همان الگوی دوستونی نامتقارن صفحهٔ بازی (صفحه + لیست حرکات قابل‌پیمایش)، اما بدون ساعت زنده و با کنترل پخش (قبلی/بعدی/شروع/پایان) در نوار پایین ستون سایدبار.

### ۳.۷ پروفایل و تنظیمات (`/profile`, `/settings`)

- **گرید:** تک‌ستونی با گروه‌بندی افقی (flowline) — دقیقاً معادل «لیست گروه‌بندی‌شده تورفته» که در سند اول (بخش ۶.۳) تعریف شد. هر گروه تنظیمات (حساب، صدا، تم، حریم خصوصی) یک منطقهٔ فضایی مجزا با فاصلهٔ عمودی مشخص است.
- **منطق کتاب:** استفاده از فاصله (نه خط یا رنگ) برای جداسازی گروه‌ها، دقیقاً همان اصل «فضا به‌عنوان عنصر گرافیکی» است — گروه‌بندی تنظیمات نیازی به قاب یا رنگ پررنگ ندارد.

### ۳.۸ جدول امتیازات (`/leaderboard`, Could)

- **گرید:** مدولار/جدولی با ستون‌های ثابت (رتبه، نام، ریتینگ، تعداد بازی). ردیف کاربر فعلی با یک نشانگر رنگی ملایم (نه پس‌زمینهٔ پررنگ) از بقیه متمایز می‌شود — کاربرد رنگ به‌عنوان عنصر سازمان‌دهنده، نه تزئین.

### ۳.۹ کنسول Staff (`/staff/*`) — دنیای «اطلاعات متراکم»

طبق سند نیازمندی (بخش ۱۷.۴)، این ناحیه همان زبان بصری اپل/فارسی را دارد اما با **تراکم اطلاعاتی بالاتر**. اصل کتاب دربارهٔ «تسلط بر فضاهای فشرده» دقیقاً همین‌جا کاربرد دارد:

- حاشیهٔ بیرونی صفحه می‌تواند کوچک‌تر از صفحات بازیکن عادی باشد، **به‌شرط** آنکه ترازبندی داخلی (جدول‌ها، فیلترها) کاملاً منظم و روی خطوط گرید ثابت باشد — تراکم فقط وقتی قابل قبول است که نظم آن بی‌نقص باشد.
- **گرید پیشنهادی صفحهٔ اصلی Staff:** سایدبار ناوبری ثابت (باریک) + ناحیهٔ محتوای اصلی مدولار برای کارت‌های KPI (تعداد گزارش باز، کاربران بن‌شده، بازی‌های زنده) + جدول‌های زیرین با فیلتر بالای هر جدول.
- **گرید صفحهٔ جزئیات** (`/staff/users/:id`, `/staff/reports/:id`): دوستونی — ستون اصلی برای جزئیات/تاریخچهٔ اقدام، ستون کناری باریک برای اقدامات سریع (بن، هشدار، escalate) و متادیتا — مشابه الگوی نامتقارن صفحهٔ بازی اما با نسبت متعادل‌تر (~۷۰/۳۰).
- جدول‌ها باید از یک گرید ستونی ثابت (نه flexbox آزاد) پیروی کنند تا چشم بتواند بین ردیف‌ها به‌سرعت اسکن کند — این دقیقاً نقطهٔ تفاوت اصلی بین UI بازیکن (که ریتم و تنفس اولویت دارد) و UI Staff (که سرعت اسکن اولویت دارد).

### ۳.۱۰ حالت تماشا (`/spectate`, Could)

- **گرید:** مشابه صفحهٔ بازی اما با ستون سایدبار متفاوت (چت تماشاگران/لیست تماشاگران به‌جای اقدامات بازیکن).

---

## ۴. اصول عرضی که در همهٔ صفحات باید رعایت شود

### ۴.۱ ریتم و جریان بین صفحات

طبق اصل «ریتم و جریان» کتاب مرجع، تجربهٔ کاربر نباید در هر صفحه از صفر شروع شود. برای این محصول:

- فاصلهٔ عمودی بین بخش‌ها (spacing) باید از یک مقیاس ثابت (همان `--space-*` سند اول) در تمام صفحات استفاده کند تا گذار از صفحه‌ای به صفحهٔ دیگر «آرام» حس شود، نه پرشی.
- صفحات با تراکم بالا (Staff) و صفحات آرام (بازیکن) باید در همان خانوادهٔ تایپوگرافی/رنگ بمانند؛ فقط **تراکم** و **اندازهٔ حاشیه** تغییر می‌کند، نه زبان بصری.

### ۴.۲ فضای تنفس در برابر تراکم اطلاعات

این محصول دو حالت متضاد دارد که باید آگاهانه مدیریت شوند:

| زمینه | نسبت فضای خالی | توجیه |
| :--- | :--- | :--- |
| صفحات بازیکن عادی (Home، Game، Result) | زیاد — برند «Peace» | حس آرامش و تمرکز؛ اولویت با تجربهٔ احساسی |
| کنسول Staff (جدول‌ها، گزارش‌ها) | کم تا متوسط | اولویت با سرعت اسکن و مقایسهٔ داده؛ اما هرگز به‌قیمت ازدحام بی‌نظم |

### ۴.۳ رنگ به‌عنوان عنصر سازمان‌دهنده (نه تزئین)

طبق فصل «رنگ به‌عنوان عنصر سازمان‌دهنده» کتاب مرجع، رنگ باید **معنا حمل کند**، نه فقط زیبایی:

- رنگ فقط برای این موارد استفاده شود: وضعیت نوبت، هشدار کیش، هشدار پایان زمان، وضعیت اتصال، ردیف کاربر فعلی در جدول، badge نقش Staff.
- هرگز رنگ به‌صورت تزئینی روی کارت‌های خنثی (مثل کارت تاریخچه) اضافه نشود — این با اصل «Deference» سند اول و اصل «رنگ به‌عنوان قاب اطلاعات» کتاب مرجع در تضاد است.

### ۴.۴ سلسله‌مراتب تصویر و اندازه

طبق اصل «تعیین نظم تصاویر» کتاب مرجع، اندازهٔ یک عنصر بصری باید بازتاب اهمیت آن باشد. در این محصول: صفحهٔ شطرنج > آواتار/نام بازیکنان > آیکون‌های اقدام > متادیتای فرعی (زمان دقیق، شناسهٔ بازی). هیچ صفحه‌ای نباید عنصری فرعی را هم‌اندازه یا بزرگ‌تر از عنصر اصلی صفحه رندر کند.

---

## ۵. پیاده‌سازی گرید با CSS خالص

```css
/* grid-tokens.css — مکمل design-tokens.css سند اول */
:root {
  --grid-gutter: var(--space-4);      /* 16px فاصله بین ستون‌ها */
  --grid-margin-mobile: var(--space-4);
  --grid-margin-desktop: var(--space-6);
  --content-max-width: 1120px;
}

/* تک‌ستونی متمرکز — Auth, Landing, Result */
.layout-focused {
  max-width: 480px;
  margin-inline: auto;
  padding-inline: var(--grid-margin-mobile);
}

/* مدولار — Home, History list, Leaderboard */
.layout-modular {
  display: grid;
  gap: var(--grid-gutter);
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  padding-inline: var(--grid-margin-mobile);
}

/* دوستونی نامتقارن — صفحه بازی، بازبینی، جزئیات Staff */
.layout-asymmetric {
  display: grid;
  gap: var(--grid-gutter);
  grid-template-columns: 1fr; /* موبایل: تک‌ستون */
}
@media (min-width: 900px) {
  .layout-asymmetric {
    grid-template-columns: 2.2fr 1fr; /* دسکتاپ: ~65/35 هم‌راستا با RTL */
    max-width: var(--content-max-width);
    margin-inline: auto;
    padding-inline: var(--grid-margin-desktop);
  }
}

/* گرید متراکم Staff — سایدبار + محتوا */
.layout-staff {
  display: grid;
  grid-template-columns: 240px 1fr;
  min-height: 100vh;
}
@media (max-width: 900px) {
  .layout-staff { grid-template-columns: 1fr; }
}
```

---

## ۶. جدول خلاصه برای مراجعهٔ سریع عامل هوش مصنوعی

| صفحه | خانوادهٔ گرید | ستون دسکتاپ | ستون موبایل | نکتهٔ کلیدی |
| :--- | :--- | :--- | :--- | :--- |
| Landing / Auth | تک‌ستونی متمرکز | ۱ | ۱ | حداکثر فضای خالی |
| Home/داشبورد | مدولار | ۲–۴ کارت | ۱ | آمار خلاصه جدا از کارت‌های CTA |
| صف بازی | تک‌ستونی متمرکز | ۱ | ۱ | حداقل محرک بصری |
| صفحهٔ بازی | نامتقارن ۲ستونه | ۶۵/۳۵ | ۱ + شیت | صفحه شطرنج همیشه غالب |
| نتیجهٔ بازی | تک‌ستونی متمرکز | ۱ | ۱ | فضای تنفس بیشتر از حد معمول |
| تاریخچه (لیست) | مدولار/جدولی | جدول | کارت عمودی | ستون‌های هم‌تراز |
| بازبینی بازی | نامتقارن ۲ستونه | ۶۵/۳۵ | ۱ + کنترل پخش | بدون ساعت زنده |
| پروفایل/تنظیمات | تک‌ستونی گروه‌بندی‌شده | ۱ | ۱ | جداسازی گروه‌ها با فاصله نه خط |
| Leaderboard | جدولی | جدول ثابت | کارت فشرده | رنگ فقط برای ردیف کاربر فعلی |
| Staff Dashboard | سایدبار + مدولار | ۲۴۰px + محتوا | تک‌ستون | تراکم بالاتر، نظم دقیق‌تر |
| Staff Detail | نامتقارن ۲ستونه | ۷۰/۳۰ | ۱ | ستون کناری = اقدامات سریع |

---

## ۷. چک‌لیست الزامی برای عامل هنگام ساخت یک صفحهٔ جدید

- [ ] قبل از کدنویسی، خانوادهٔ گرید صفحه را از جدول بخش ۶ انتخاب کن — گرید دلبخواه نساز.
- [ ] شش عنصر گرید (ستون، مدول، حاشیه، منطقهٔ فضایی، فلولاین، مارکر) را برای همان صفحه نام‌گذاری کن.
- [ ] بزرگ‌ترین عنصر گرید باید همان عنصری باشد که از نظر محصول مهم‌ترین است (نه صرفاً اولین چیزی که کدنویسی شده).
- [ ] نسبت فضای خالی را با توجه به زمینهٔ صفحه (بازیکن آرام / Staff متراکم) از بخش ۴.۲ تنظیم کن.
- [ ] رنگ فقط برای انتقال معنا/وضعیت استفاده شود، نه تزئین کارت‌های خنثی.
- [ ] در موبایل، گرید نامتقارن به تک‌ستون + شیت/کشو تبدیل شود، نه فشرده‌سازی همان دو ستون.
- [ ] جهت RTL و منطق `inline-start/end` در تمام مقادیر `grid-template-columns` و margin رعایت شود.
- [ ] فاصله‌ها فقط از `--grid-gutter` و `--space-*` تعریف‌شده در توکن‌ها استفاده کنند؛ عدد دستی ممنوع.

---

*این سند بر پایهٔ اصول عمومی گرید طراحی‌شده در کتاب Layout Essentials (بث توندرو) تدوین شده و آن اصول را — بدون کپی متن یا تصاویر کتاب — برای معماری صفحات این محصول خاص بازتفسیر می‌کند.*
